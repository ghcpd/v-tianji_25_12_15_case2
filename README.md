# Personal Finance Tracker

This is a small React + Vite app to track income and expenses. It includes unit tests (Vitest + Testing Library) and E2E tests (Cypress).

Quick commands:

```
npm install
npm run dev
npm test
npm run cypress:open
```
